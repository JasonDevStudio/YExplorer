<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ViewModels="clr-namespace:YMauiExplorer.ViewModels"
             x:Class="YMauiExplorer.MainPage">

	<ContentPage.BindingContext>
		<ViewModels:MainViewModel/>
	</ContentPage.BindingContext>

    <ScrollView>
        <VerticalStackLayout
            Spacing="25"
            Padding="30,0"
            VerticalOptions="Center">

            <StackLayout Margin="10,5">
                <Label HorizontalTextAlignment="Start" VerticalTextAlignment="Center"  Text="目录："/>
                <Picker SelectedItem="{Binding SelectedDir}" ItemsSource="{Binding DirPaths}" />
                <Button Text="加载" Command="{Binding LoadDirCommand}"></Button>
            </StackLayout>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
